{% extends 'base.html.twig' %}

{% block title %}Absence index{% endblock %}

{% block body %}
    <h1>Absence index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CreatedAt</th>
                <th>Date</th>
                <th>CreatedBy</th>
                <th>Du</th>
                <th>Au</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for absence in absences %}
            <tr>
                <td>{{ absence.id }}</td>
                <td>{{ absence.createdAt ? absence.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ absence.date ? absence.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ absence.createdBy }}</td>
                <td>{{ absence.du ? absence.du|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ absence.au ? absence.au|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_absence_show', {'id': absence.id}) }}">show</a>
                    <a href="{{ path('app_absence_edit', {'id': absence.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_absence_new') }}">Create new</a>
{% endblock %}
