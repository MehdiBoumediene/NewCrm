{% extends 'base.html.twig' %}

{% block title %}Tuteur{% endblock %}

{% block body %}
    <h1>Tuteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tuteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tuteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ tuteur.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ tuteur.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ tuteur.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ tuteur.telephone }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ tuteur.createdAt ? tuteur.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ tuteur.createdBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tuteur_index') }}">back to list</a>

    <a href="{{ path('app_tuteur_edit', {'id': tuteur.id}) }}">edit</a>

    {{ include('tuteur/_delete_form.html.twig') }}
{% endblock %}
